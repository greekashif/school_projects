import javax.swing.*;
import java.awt.*;
import java.awt.event.*;
import java.awt.image.*;
import javax.imageio.*;
import java.io.*;

public class P4Arcade {

	public static void main(String[] args) {
        
		ButtonPanel bp = new ButtonPanel();
      
/*        BufferedImage cursor = null;
        try
        {
            cursor = ImageIO.read(new File("arcade_main.jpg"));
            System.out.println("File loaded");
        }
        catch(IOException e)
        {
            System.out.println("Error\n");
            System.exit(1);
        }
//        ButtonPanel.setLayout(new BorderLayout.Y_AXIS);
       
        /*
        String imgfile = args[0];

        BufferedImage cursor = null;
        try
        {
            cursor = ImageIO.read(new File("arcade_main.jpg"));
            System.out.println("File loaded");
        }
        catch(IOException e)
        {
            System.out.println("Error\n");
            System.exit(1);
        }

        BufferedImage cursor1 = new BufferedImage(16,16, BufferedImage.TYPE_INT_ARGB);
        
        cursor1.createGraphics();

        Cursor cursor_ = Toolkit.getDefaultToolkit().createCustomCursor(
            cursor1, new Point(0,0), "My Cursor");
        f.setCursor(cursor_);    
        */
     
	}

}
